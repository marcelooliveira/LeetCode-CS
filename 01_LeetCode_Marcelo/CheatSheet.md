Aqui está a tabela completa em Markdown reunindo todos os "LeetCodes Clássicos" organizados por seus respectivos padrões, as estruturas de dados adequadas no C# (.NET), a lógica-chave e o passo a passo resumido de cada solução:

| LeetCode Title | LeetCode Pattern | Data Structures (C#) | Key Logic (Lógica-Chave) | Passos Simples do Algoritmo |
| :--- | :--- | :--- | :--- | :--- |
| **Maximum Subarray** | Sliding Window / Kadane | `int[]` | Manter a soma atual e redefinir se ela se tornar negativa. | 1. Varra o array acumulando a soma em `currentSum`. <br>2. Se `currentSum > maxSum`, atualize `maxSum`. <br>3. Se `currentSum < 0`, resete `currentSum = 0`. |
| **Longest Substring Without Repeating Characters** | Sliding Window | `HashSet<char>` ou `Dictionary<char, int>` | Janela expansível que encolhe pela esquerda ao encontrar duplicados. | 1. Use dois ponteiros (`left`, `right`). <br>2. Avance `right` e adicione caracteres ao Set. <br>3. Se achar duplicado, mova `left` para a direita e remova do Set até o duplicado sumir. <br>4. Calcule o tamanho máximo (`right - left + 1`). |
| **Minimum Size Subarray Sum** | Sliding Window | `int[]` | Janela dinâmica que encolhe assim que atinge a soma alvo. | 1. Expanda a janela somando elementos com o ponteiro `right`. <br>2. Enquanto a soma for $\ge$ alvo, registre o menor comprimento e encolha a janela movendo `left` para a direita. |
| **Two Sum II - Input Array Is Sorted** | Two Pointers | `int[]` | Ajustar ponteiros nas extremidades com base na comparação com o alvo. | 1. Defina `left = 0` e `right = array.Length - 1`. <br>2. Se a soma for igual ao alvo, retorne os índices. <br>3. Se for menor, mova `left++`. Se for maior, mova `right--`. |
| **3Sum** | Two Pointers | `List<IList<int>>`, `int[]` | Ordenar o array e usar dois ponteiros para cada elemento fixo, evitando duplicatas. | 1. Ordene o array. <br>2. Use um loop `i` fixo. Se for duplicado, pule. <br>3. Aplique Two Pointers no restante do array (`i + 1` até o fim) para achar a soma `-nums[i]`. |
| **Container With Most Water** | Two Pointers | `int[]` | Calcular área e mover o ponteiro que aponta para a menor altura. | 1. Inicie ponteiros nas pontas (`left` e `right`). <br>2. Calcule a área usando a menor altura entre eles multiplied pela distância. <br>3. Atualize o máximo de água. <br>4. Mova o ponteiro menor para dentro. |
| **Linked List Cycle** | Fast & Slow Pointers | Classe customizada `ListNode` | Se houver um ciclo, o ponteiro rápido alcançará o lento por trás. | 1. Inicialize `slow` e `fast` na cabeça (`head`). <br>2. Mova `slow = slow.next` e `fast = fast.next.next`. <br>3. Se eles se encontrarem, há ciclo. Se `fast` chegar ao fim (`null`), não há. |
| **Happy Number** | Fast & Slow Pointers | `int` | Detectar ciclos na sequência de soma dos quadrados dos dígitos. | 1. Use a lógica do ciclo: trate o número atual como nó e a soma dos quadrados de seus dígitos como o próximo nó (`next`). <br>2. Avance o rápido 2x e o lento 1x. <br>3. Se pararem no número $1$, é feliz. Se colidirem fora do $1$, é ciclo infinito (não feliz). |
| **Palindrome Linked List** | Fast & Slow Pointers | `ListNode`, `Stack<int>` (opcional) | Encontrar o meio, reverter a segunda metade e comparar com a primeira. | 1. Use ponteiros rápido e lento para achar o meio da lista. <br>2. Inverta a segunda metade da lista in-place. <br>3. Compare elemento por elemento a primeira metade com a segunda metade invertida. |
| **Merge Intervals** | Merge Intervals | `int[][]`, `List<int[]>` | Ordenar por início e mesclar se o início do atual for menor ou igual ao fim do anterior. | 1. Ordene os intervalos pelo tempo de início. <br>2. Adicione o primeiro intervalo à lista de resultado. <br>3. Para cada intervalo subsequente, se sobrepor ao último adicionado, funda-os (atualize o fim). Senão, adicione-o como novo. |
| **Insert Interval** | Merge Intervals | `List<int[]>` | Dividir em 3 partes: antes do novo intervalo, a mesclagem, e após o novo intervalo. | 1. Adicione todos os intervalos que terminam antes do início do novo. <br>2. Mescle todos os intervalos que se sobrepõem ao novo atualizando o início/fim. <br>3. Insira o intervalo mesclado. <br>4. Adicione o restante dos intervalos. |
| **Non-overlapping Intervals** | Merge Intervals | `int[][]` | Ordenar pelo tempo de término (Ganancioso/Greedy) e remover o que termina mais tarde. | 1. Ordene os intervalos pelo tempo de término (`end`). <br>2. Mantenha o fim do último intervalo válido. <br>3. Se o próximo intervalo começar antes do fim do anterior, incremente o contador de remoção. Senão, atualize o fim válido. |
| **Missing Number** | Cyclic Sort | `int[]` | Colocar cada número no seu índice correto (`nums[i]` deve estar em `i`). | 1. Percorra o array. Se `nums[i] < n` e `nums[i] != i`, troque-o com o elemento no índice `nums[i]`. <br>2. Após ordenar, o primeiro índice `i` onde `nums[i] != i` é o número ausente. |
| **Find All Numbers Disappeared** | Cyclic Sort | `int[]`, `List<int>` | Colocar elementos em seus índices corretos e identificar os desalinhados. | 1. Ordene ciclicamente de forma que o número `x` vá para o índice `x - 1`. <br>2. Percorra novamente: qualquer índice `i` onde `nums[i] != i + 1` indica que o número `i + 1` está faltando. |
| **First Missing Positive** | Cyclic Sort | `int[]` | Mapear inteiros positivos $1 \dots n$ para os índices $0 \dots n-1$. | 1. Ignore negativos e números maiores que $n$. <br>2. Ordene ciclicamente colocando `nums[i]` no índice `nums[i] - 1`. <br>3. Varra o array: o primeiro índice onde `nums[i] != i + 1` revela o menor positivo ausente. |
| **Reverse Linked List** | In-place Reversal | `ListNode` | Alterar as referências de direção dos ponteiros sem alocar nova memória. | 1. Defina ponteiros `prev = null` e `current = head`. <br>2. No loop, salve `next = current.next`. <br>3. Inverta o link: `current.next = prev`. <br>4. Avance: `prev = current`, `current = next`. |
| **Reverse Nodes in k-Group** | In-place Reversal | `ListNode` | Reverter blocos de tamanho $k$ e costurar as pontas corretamente. | 1. Verifique se existem pelo menos $k$ nós adiante. <br>2. Se sim, reverta esses $k$ nós localmente. <br>3. Conecte a cabeça revertida ao resultado e repita recursivamente para o próximo grupo de $k$. |
| **Reverse Linked List II** | In-place Reversal | `ListNode` | Localizar a sublista entre os índices `left` e `right` e revertê-la in-place. | 1. Mova um ponteiro auxiliar até o nó imediatamente anterior à posição `left`. <br>2. Inverta as conexões dos nós um por um até alcançar a posição `right`. <br>3. Reconecte as pontas externas da lista com a seção revertida. |
| **Binary Tree Level Order Traversal** | Tree BFS | `Queue<TreeNode>`, `List<IList<int>>` | Processar árvore nível por nível usando uma fila e controlando o tamanho do nível. | 1. Enfileire a raiz. <br>2. Enquanto a fila não estiver vazia, pegue o tamanho atual dela (`size`). <br>3. Processe exatamente `size` elementos tirando-os da fila e enfileirando seus filhos. <br>4. Adicione esse nível ao resultado. |
| **Populating Next Right Pointers** | Tree BFS | `Queue<TreeNode>` (ou ponteiros de nível) | Conectar nós adjacentes no mesmo nível horizontal da árvore. | 1. Use BFS padrão com fila para obter os nós por nível. <br>2. Dentro de cada nível, aponte o `.next` de cada nó para o próximo elemento da fila. <br>3. O último nó do nível aponta para `null`. |
| **Binary Tree Zigzag Level Order** | Tree BFS | `Queue<TreeNode>`, `LinkedList<int>` | Alternar a direção de inserção (esquerda-direita / direita-esquerda) a cada nível. | 1. Execute um BFS de nível clássico. <br>2. Use uma flag booleana `leftToRight`. <br>3. Se for direta, insira no fim da lista do nível; se invertida, insira no início (usando `AddFirst`). <br>4. Inverta a flag a cada nível. |
| **Path Sum** | Tree DFS | Recursão (Call Stack) | Subtrair o valor do nó atual do valor alvo até chegar a uma folha. | 1. Se o nó for nulo, retorne falso. <br>2. Se for folha (sem filhos), retorne se o valor do nó é igual ao alvo restante. <br>3. Chame recursivamente para os filhos esquerdo e direito passando `targetSum - node.val`. |
| **Diameter of Binary Tree** | Tree DFS | Recursão (Call Stack) | O diâmetro passando por um nó é a soma das alturas da subárvore esquerda e direita. | 1. Crie uma função que calcula a altura de uma árvore. <br>2. Em cada nó visitado, calcule a soma das alturas esquerda e direita. <br>3. Atualize uma variável global/referenciada com o maior valor encontrado. <br>4. Retorne a altura local (`Math.Max(left, right) + 1`). |
| **Validate Binary Search Tree** | Tree DFS | Recursão (Call Stack) | Garantir que todos os nós à esquerda sejam menores e todos à direita sejam maiores que o nó pai. | 1. Use uma função recursiva que aceita um nó, um valor mínimo (`min`) e máximo (`max`). <br>2. O valor do nó atual deve estar estritamente entre `min` e `max`. <br>3. Chame recursivamente atualizando os limites para os filhos. |
| **Find Median from Data Stream** | Two Heaps | `PriorityQueue<int, int>` (Min e Max Heaps) | Dividir os números: metade menor no Max-Heap, metade maior no Min-Heap. | 1. Insira novos elementos no Max-Heap (números menores). <br>2. Mova o maior do Max-Heap para o Min-Heap (números maiores) para balancear. <br>3. Se o Min-Heap ficar maior, jogue o menor dele de volta ao Max-Heap. <br>4. Se tamanhos forem iguais, a mediana é a média dos topos; senão, é o topo do Max-Heap. |
| **IPO** | Two Heaps | `PriorityQueue<int, int>` (Min e Max Heaps) | Selecionar iterativamente os projetos mais lucrativos disponíveis com o capital atual. | 1. Coloque todos os projetos em um Min-Heap ordenados pelo capital mínimo exigido. <br>2. Retire do Min-Heap todos os projetos acessíveis com o capital atual e insira-os em um Max-Heap ordenados pelo lucro. <br>3. Pegue o topo do Max-Heap (maior lucro), adicione ao capital atual. Repita $k$ vezes. |
| **Sliding Window Median** | Two Heaps | `PriorityQueue<int, int>` (ou SortedSet personalizado) | Manter os dois heaps balanceados enquanto a janela desliza pelo array. | 1. Adicione o novo elemento que entra na janela nos Heaps (mesmo princípio do *Median Finder*). <br>2. Remova o elemento que sai da janela (exige remoção preguiçosa ou estrutura que suporte remoção em $O(\log n)$). <br>3. Rebalanceie os Heaps e leia a mediana. |
| **Subsets** | Subsets (Backtracking) | `List<int>`, `List<IList<int>>` | Decidir recursivamente incluir ou não incluir cada elemento no subconjunto atual. | 1. Comece com uma lista vazia `path`. <br>2. No método recursivo, adicione uma cópia de `path` ao resultado global. <br>3. Faça um loop de `i` até o fim dos dados: adicione `nums[i]` ao `path`, chame a recursão para `i + 1`, e remova `nums[i]` (backtrack). |
| **Combinations** | Subsets (Backtracking) | `List<int>`, `List<IList<int>>` | Gerar combinações de tamanho fixo $k$ a partir de números de $1$ a $n$. | 1. Use recursão com um índice inicial. <br>2. Se o tamanho de `path` atingir $k$, adicione-o ao resultado final e retorne. <br>3. Varra os números de `start` a `n`: adicione o número a `path`, faça a chamada recursiva para o próximo índice, e depois remova-o. |
| **Letter Combinations of a Phone** | Subsets (Backtracking) | `Dictionary<char, string>`, `List<string>` | Mapear dígitos para letras e gerar todas as combinações recursivamente. | 1. Defina o mapeamento dos botões do telefone. <br>2. Use uma função recursiva que recebe o índice do dígito atual. <br>3. Se o tamanho do texto atingir o tamanho dos dígitos de entrada, salve o resultado. <br>4. Senão, itere sobre as letras correspondentes ao dígito atual, adicione uma letra e avance na recursão. |
| **Search in Rotated Sorted Array** | Modified Binary Search | `int[]` | Determinar qual metade do array está ordenada e verificar se o alvo está nela. | 1. Calcule o `mid`. <br>2. Verifique se a metade esquerda está ordenada (`nums[low] <= nums[mid]`). <br>3. Se sim, veja se o alvo está nela e ajuste as bordas. <br>4. Caso contrário, a metade direita está ordenada; repita a lógica de busca nela. |
| **Find First and Last Position** | Modified Binary Search | `int[]` | Fazer duas buscas binárias: uma para o limite esquerdo e outra para o direito. | 1. Execute uma busca binária modificada. Ao encontrar o alvo, continue procurando à esquerda (atualize `high = mid - 1`) para achar a primeira posição. <br>2. Repita a lógica, mas ao encontrar continue à direita para achar a última posição. |
| **Peak Index in a Mountain Array** | Modified Binary Search | `int[]` | Encontrar o ponto de virada onde o elemento seguinte passa a ser menor. | 1. Calcule o `mid`. <br>2. Se `arr[mid] < arr[mid + 1]`, significa que estamos subindo a montanha; mova `low = mid + 1`. <br>3. Caso contrário, estamos na descida; mova `high = mid`. <br>4. No fim, `low` apontará para o pico. |
| **Kth Largest Element in an Array** | Top 'K' Elements | `PriorityQueue<int, int>` | Manter um Min-Heap de tamanho máximo $k$. | 1. Adicione elementos ao Min-Heap. <br>2. Se o tamanho do Heap passar de $k$, remova (`Dequeue`) o menor elemento. <br>3. Ao final de todo o array, o topo do Heap conterá o $k$-ésimo maior elemento. |
| **Top K Frequent Elements** | Top 'K' Elements | `Dictionary<int, int>`, `PriorityQueue<int, int>` | Contar frequências e usar um Heap priorizado pela menor frequência. | 1. Popule um dicionário com a contagem de cada número. <br>2. Insira os números em um Min-Heap usando sua frequência como prioridade. <br>3. Garanta que o Heap mantenha tamanho máximo $k$ descartando elementos menos frequentes. |
| **K Closest Points to Origin** | Top 'K' Elements | `PriorityQueue<int[], double>` | Armazenar os pontos em um Max-Heap com prioridade baseada na distância euclidiana. | 1. Calcule a distância de cada ponto à origem ($x^2 + y^2$). <br>2. Insira no Max-Heap. <br>3. Se o tamanho exceder $K$, descarte o ponto mais distante. <br>4. Restarão os $K$ pontos mais próximos. |
| **Merge k Sorted Lists** | K-way Merge | `PriorityQueue<ListNode, int>` | Inserir a cabeça de todas as listas em um Min-Heap e extrair sucessivamente o menor. | 1. Adicione os primeiros nós (`head`) de cada uma das $k$ listas em um Min-Heap. <br>2. Remova o nó com menor valor do Heap e anexe-o à lista de resposta. <br>3. Se o nó removido tiver um sucessor (`node.next`), coloque o sucessor no Heap. Repita até o Heap esvaziar. |
| **Find K Pairs with Smallest Sums** | K-way Merge | `PriorityQueue<int[], int>` | Usar o Heap para extrair as menores somas possíveis combinando dois arrays ordenados. | 1. Coloque no Min-Heap os pares formados pelo primeiro elemento de `nums1` combinado com cada elemento de `nums2` (ou vice-versa). <br>2. Remova o par de menor soma. <br>3. Insira o próximo par candidato avançando o índice do elemento do array correspondente. <br>4. Repita até obter os $k$ pares. |
| **Smallest Range Covering Elements** | K-way Merge | `PriorityQueue<Element, int>` | Encontrar o menor intervalo contendo pelo menos um número de cada uma das $k$ listas. | 1. Insira o primeiro elemento de cada uma das $k$ listas em um Min-Heap. Mantenha o controle do valor máximo atualmente no Heap. <br>2. Remova o menor elemento. O intervalo atual é dado por `[menor_removido, maximo_atual]`. <br>3. Se for menor que o intervalo global histórico, atualize-o. <br>4. Insira o próximo elemento da lista que pertencia o número removido. |
| **Course Schedule** | Topological Sort | `Dictionary<int, List<int>>`, `int[]`, `Queue<int>` | Algoritmo de Kahn: detectar se existe um ciclo de dependências no grafo. | 1. Calcule o grau de entrada (in-degree) de cada curso. <br>2. Coloque em uma fila todos os cursos com in-degree 0 (sem pré-requisitos). <br>3. Processe a fila: para cada curso removido, reduza o in-degree de seus vizinhos. Se algum vizinho zerar, enfileire-o. <br>4. Se conseguiu processar todos os cursos, é possível concluir; senão há um ciclo. |
| **Course Schedule II** | Topological Sort | `Dictionary<int, List<int>>`, `int[]`, `Queue<int>`, `int[]` (res) | Retornar a ordem exata de processamento dos nós sem dependências pendentes. | 1. Idêntico ao *Course Schedule*, mas salve cada curso removido da fila em um array de resultado. <br>2. Se ao final o tamanho do resultado for igual ao número total de cursos, retorne a lista. Senão, retorne um array vazio (ciclo detectado). |
| **Alien Dictionary** | Topological Sort | `Dictionary<char, HashSet<char>>`, `Dictionary<char, int>` (grau), `StringBuilder` | Construir dependências de letras a partir de palavras adjacentes no dicionário ordenado. | 1. Compare palavras vizinhas para achar a primeira letra diferente (isso dá a relação `letra A -> letra B`). <br>2. Monte o grafo e o array de graus de entrada para cada caractere único. <br>3. Execute o Algoritmo de Kahn. Se a string final contiver todos os caracteres únicos, a ordem é válida. |
| **Partition Equal Subset Sum** | Dynamic Programming | `bool[]` (1D DP) | Reduzir o problema para encontrar se um subconjunto soma exatamente a metade da soma total. | 1. Calcule a soma total. Se for ímpar, retorne falso. <br>2. Defina o alvo (`target = soma / 2`). <br>3. Crie um array booleano `dp` de tamanho `target + 1` com `dp[0] = true`. <br>4. Para cada número, atualize o array `dp` de trás para frente (`target` até `num`): `dp[i] = dp[i] || dp[i - num]`. |
| **Target Sum** | Dynamic Programming | `Dictionary<string, int>` ou `int[]` | Converter para o problema de soma de subconjuntos e achar o número de maneiras de obter a soma. | 1. O problema equivale a achar subconjuntos que somam `(alvo + total) / 2`. <br>2. Se essa divisão não for inteira ou o alvo for impossível, retorne 0. <br>3. Use um array DP unidimensional onde `dp[i]` guarda o número de combinações para somar `i` (similar ao Coin Change). |
| **Coin Change** | Dynamic Programming | `int[]` | Calcular o número mínimo de moedas para cada valor de $1$ até o valor total. | 1. Crie um array `dp` de tamanho `amount + 1` preenchido com um valor alto (`amount + 1`). Defina `dp[0] = 0`. <br>2. Para cada valor de $1$ a `amount`: teste cada moeda. <br>3. Se a moeda for menor ou igual ao valor atual, faça `dp[i] = Math.Min(dp[i], dp[i - moeda] + 1)`. |